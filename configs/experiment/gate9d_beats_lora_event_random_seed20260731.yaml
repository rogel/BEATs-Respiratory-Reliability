gate: 9D
name: gate9d_beats_lora_event_random_seed20260731
seed: 20260731
train_value: train_fit
validation_value: validation_select
label_column: binary_label_id
batch_size: 8
samples_per_epoch: 7515
num_workers: 4
sampling:
  mode: event_random
augmentation:
  gain_min: 0.8
  gain_max: 1.2
  noise_probability: 0.5
  noise_snr_min_db: 12.0
  noise_snr_max_db: 30.0
  shift_probability: 0.5
  max_shift_fraction: 0.1
optimizer:
  name: adamw
  adaptation_learning_rate: 0.0001
  head_learning_rate: 0.0001
  weight_decay: 0.01
schedule:
  name: warmup_cosine
  warmup_epochs: 2
  max_epochs: 12
  early_stopping_patience: 4
consistency:
  enabled: true
  name: jensen_shannon
  max_weight: 0.4
  warmup_epochs: 12
projection_drift_anchor:
  enabled: false
  max_weight: 0.0
  warmup_epochs: 3
