seed: 20260727
device_preference: mps
precision: fp32
split_column: development_split
train_value: train
validation_value: validation
batch_size: 64
num_workers: 0
augmentation:
  enabled: true
optimizer:
  name: adamw
  learning_rate: 0.0003
  weight_decay: 0.0001
schedule:
  name: cosine
  warmup_epochs: 5
  max_epochs: 150
  early_stopping_patience: 20
loss:
  class_weight_power: -0.5
consistency:
  enabled: true
  name: jensen_shannon
  max_weight: 0.4
  warmup_epochs: 20
gate:
  min_score_gain_points: 1.0
  preferred_score_gain_points: 1.5
  require_macro_f1_gain: true
  require_repconv_advantage: true
  fp32_fold_max_abs_error: 1.0e-5
